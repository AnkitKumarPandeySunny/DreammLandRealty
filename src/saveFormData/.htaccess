RewriteEngine On

# Remove .php extension from URL
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^/]+)$ $1.php [L]

# Redirect from .php to extensionless (optional)
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.php\sHTTP
RewriteRule ^ %1 [R=301,L]
